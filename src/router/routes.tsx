import React from "react";
import { Route, Routes } from "react-router";
import { Home } from "./routePaths";

const Router = () => {
  return <Routes>{/* NOTE: <Route path={Card} element={Card} /> */}</Routes>;
};

export default Router;
